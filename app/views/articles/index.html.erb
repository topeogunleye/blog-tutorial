<h1>This is our index page</h1>
<ul>
  <% @articles.each do |article| %>
  
  <strong><%= article.title%></strong>
<p><%= article.content %></p>
<%= link_to "show", article_path(article.id) %>
<%= button_to "Delete", article_path(article.id), method: :delete %>
  <%end%>
</ul>
<%= link_to "New", articles_new_path %>